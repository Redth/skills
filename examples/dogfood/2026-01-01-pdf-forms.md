---
generated_by: skill-reflect
schema: 2
date: 2026-01-01
skill: pdf-forms
source_repo: unknown
install_scope: unknown
provenance_source: unknown
provenance_confidence: None
sessions_reviewed: 1
review_mode: artifact
detail_level: strict
remote_eligible: false
consent: review-only
---

# Field feedback for `pdf-forms`

## Summary

Reviewed `pdf-forms` across one synthetic session. Found three findings: one High
(stale flag in the `fill` subcommand), one Medium (undocumented change to the
`validate` output schema), and one Medium trigger-miss (natural phrasing for the
fill task did not invoke the skill). The dominant pattern is stale guidance — the
skill's documented interface has diverged from the current tool version.

## Findings

### 1 Stale `--flatten` flag in `fill` subcommand  ·  High / Likely / Worked-around

- **Category:** wrong-or-stale-guidance
- **What happened:** The agent invoked the `fill` subcommand using the `--flatten`
  flag as documented in the skill. The tool rejected the flag with an "unknown option"
  error indicating the flag was renamed in a major version update. The agent retried
  the same invocation three times without success, then abandoned the skill's approach
  and used a separate external tool to complete the flatten step.
- **Signal:** The `fill` subcommand exited with a non-zero status on each of three
  consecutive retries; the error output named the flag as unknown and suggested the
  renamed form. The agent did not update the invocation across retries, indicating the
  skill's guidance gave no hint of the rename. Task was ultimately completed via an
  external workaround tool.
- **Proposed fix:** Replace `--flatten` with `--flatten-fields` throughout the skill's
  `fill` subcommand guidance and examples. Add a version note (e.g. "v2.0+") so users
  on older versions understand the change. Consider adding a preflight check or a
  compatibility note at the top of the `fill` section.
- **Proposed eval:**
  ```json
  {
    "id": "pf-flatten-stale",
    "prompt": "Use pdf-forms to fill a multi-field PDF form and produce a flattened output file.",
    "must_contain": ["--flatten-fields"],
    "must_not_contain": ["--flatten "]
  }
  ```

### 2 `validate` subcommand output schema undocumented  ·  Medium / Likely / Worked-around

- **Category:** wrong-or-stale-guidance
- **What happened:** After the fill step, the agent invoked `pdf-forms validate` to
  check the completed form. The skill's documentation describes a response object with
  a `valid` boolean and an `errors` array. The actual tool output uses a different
  schema with a `status` string, a `fields_validated` count, and a `warnings` array.
  The agent retried the validate invocation four times without meaningful variation,
  unable to reconcile the documented schema with the actual output. On the final retry
  the agent abandoned the documented parse path and read the raw response directly.
- **Signal:** Four consecutive validate invocations with identical arguments; each
  produced output that matched an undocumented schema rather than the documented one.
  The agent's reasoning trace explicitly noted the mismatch on the third retry.
- **Proposed fix:** Update the `validate` subcommand documentation to reflect the
  current output schema (`status`, `fields_validated`, `warnings`). Remove or strike
  through the obsolete `valid`/`errors` schema. Add a worked example showing the
  actual response and how to interpret each field.
- **Proposed eval:**
  ```json
  {
    "id": "pf-validate-schema",
    "prompt": "Use pdf-forms to validate a completed PDF form and interpret the validation result.",
    "must_contain": ["fields_validated", "warnings"],
    "must_not_contain": ["\"valid\":", "\"errors\":"]
  }
  ```

### 3 Trigger miss for fill-task phrasing  ·  Medium / Possible / Worked-around

- **Category:** trigger-problem
- **What happened:** The user's request described filling an expense PDF form for
  submission. The `pdf-forms` skill was not triggered; the agent instead began manual
  field-editing steps before eventually invoking the skill explicitly. The skill's
  trigger description uses "PDF forms" vocabulary but does not include phrasing
  variants around "expense form", "fill out", or "submit PDF", which are natural ways
  a user might describe the same task.
- **Signal:** The session shows the agent proceeding without invoking `pdf-forms`
  immediately on the fill request; the skill was only used after an explicit naming
  step. Confidence is `Possible` because the session source is synthetic and causal
  certainty is limited.
- **Proposed fix:** Extend the skill's trigger description to include phrasing variants
  such as "fill out a PDF", "complete a form", "expense form", and "submit PDF fields".
  Test with at least three natural phrasing variants beyond the current vocabulary.
- **Proposed eval:**
  ```json
  {
    "query": "Fill out the quarterly expense PDF form and flatten the fields for submission.",
    "should_trigger": true
  }
  ```

## Privacy

This report was scrubbed of names, paths, secrets, product/domain specifics, and
verbatim excerpts. Values are paraphrased and any reproduction details are recast as
an invented, analogous scenario. Created under explicit local-write authorization.

## Routing

Suggested destination: local only. No remote filing suggested.
